-- generated by dbdsgnr.appspot.com

CREATE TABLE `product` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `desc` TEXT,
  `set` TEXT,
  `features` TEXT,
  `image` TINYTEXT,
  `url` TEXT,
  `title` TINYTEXT,
  `price` INT,
  `instore` INT,
  `anonse` TEXT,
  PRIMARY KEY  (`id`)
);

CREATE TABLE `catalog` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `link` TEXT,
  `url` TEXT,
  `desc` TEXT,
  PRIMARY KEY  (`id`)
);

CREATE TABLE `router_a` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `prodid` INT,
  `catid` INT,
  PRIMARY KEY  (`id`)
);

CREATE TABLE `delivery` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `link` TEXT,
  PRIMARY KEY  (`id`)
);

CREATE TABLE `payment` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `link` TEXT,
  PRIMARY KEY  (`id`)
);

CREATE TABLE `orders` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `person` TEXT,
  `tel` TEXT,
  `email` TEXT,
  `address` TEXT,
  `deliveryid` INT,
  `paymentid` INT,
  `sysdate` DATETIME,
  `sign` TEXT,
  PRIMARY KEY  (`id`)
);

CREATE TABLE `item` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `productid` INT,
  `title` TEXT,
  `count` INT,
  `price` INT,
  `orderid` INT,
  PRIMARY KEY  (`id`)
);



ALTER TABLE `router_a` ADD CONSTRAINT `router_a_fk1` FOREIGN KEY (`prodid`) REFERENCES product(`id`);
ALTER TABLE `router_a` ADD CONSTRAINT `router_a_fk2` FOREIGN KEY (`catid`) REFERENCES catalog(`id`);


ALTER TABLE `orders` ADD CONSTRAINT `orders_fk1` FOREIGN KEY (`deliveryid`) REFERENCES delivery(`id`);
ALTER TABLE `orders` ADD CONSTRAINT `orders_fk2` FOREIGN KEY (`paymentid`) REFERENCES payment(`id`);
ALTER TABLE `item` ADD CONSTRAINT `item_fk1` FOREIGN KEY (`orderid`) REFERENCES orders(`id`);

